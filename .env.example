# Environment Variables for SuperFill Extension

# Enable debug logging in production builds (optional)
# By default, debug logs are only shown in development mode (bun dev)
# Set this to 'true' to enable debug logs in production builds
# WXT_DEBUG=true
